% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EXTENSION_UTILS.R
\name{.fixed_size_merge}
\alias{.fixed_size_merge}
\title{merge fixed-size 3d windows}
\usage{
.fixed_size_merge(
  codon_df,
  residue_df,
  patch_mode,
  max_patch,
  dist_cutoff,
  merge_type,
  pdb_info_sets,
  only_exposed_in_patch = TRUE
)
}
\arguments{
\item{codon_df}{data frame of codons with columns \code{codon_id}, \code{exposed}, \code{exposed_count}, and \code{codon_patch}}

\item{residue_df}{data frame of residues with columns \code{codon_id}, \code{residue_id}, \code{codon_patch}, \code{pdb}, and \code{exposed}}

\item{patch_mode}{character, either \code{"codon"} or \code{"residue"}}

\item{max_patch}{integer. maximum patch size (number of codons/residues)}

\item{dist_cutoff}{numeric. maximum distance in angstroms (default NA = no cutoff)}

\item{merge_type}{character. either \code{"distance"} or \code{"exposure_distance"}}

\item{pdb_info_sets}{list of pdb_info objects, each with residue-wise distance matrices}

\item{only_exposed_in_patch}{logical. if TRUE, all residues in patch must be exposed;
if FALSE, only seed codons must be exposed}
}
\value{
updated \code{codon_df} with rebuilt \code{codon_patch} assignments
}
\description{
builds codon- or residue-level fixed size patches across one or more pdbs.
handles multimers and multi-pdb sets by unifying residue neighborhoods into
a consistent codon-level environment.
}
\keyword{internal}
