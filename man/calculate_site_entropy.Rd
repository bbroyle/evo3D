% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ANALYSIS_UTILS.R
\name{calculate_site_entropy}
\alias{calculate_site_entropy}
\title{Calculate per-site (codon-position) Shannon entropy}
\usage{
calculate_site_entropy(
  msa_info_sets,
  residue_df,
  valid_aa_only = TRUE,
  seqtype = "nucleotide",
  gencode = 1
)
}
\arguments{
\item{msa_info_sets}{Named list of MSA info objects containing \code{msa_mat}.}

\item{residue_df}{Data frame of residue/codon annotations. Must include
\code{msa} (MSA id) and \code{codon_id} (e.g., \code{"12_msa1"}).}

\item{valid_aa_only}{Logical; if \code{TRUE} (default), compute entropy using
only standard amino acid characters.}

\item{seqtype}{Character; \code{"nucleotide"} or \code{"peptide"}.}

\item{gencode}{Integer genetic code used by \code{seqinr::translate()} when
\code{seqtype = "nucleotide"} (default 1).}
}
\value{
\code{residue_df} with a new column \code{site_entropy}, containing
  per-position Shannon entropy values (base 2). Rows without a matching
  \code{codon_id} remain \code{NA}.
}
\description{
Computes Shannon entropy at each aligned position for one or more MSAs and
merges results into \code{residue_df} by \code{codon_id}. If \code{seqtype} is
\code{"nucleotide"}, sequences are translated to amino acids before entropy is
computed.
}
\details{
This is typically called downstream of MSA–structure mapping, where
\code{residue_df} contains \code{msa}, \code{codon_id}, and other per-residue
annotations.
}
\keyword{internal}
