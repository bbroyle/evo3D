% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ANALYSIS_UTILS.R
\name{summarize_stat_at_patch}
\alias{summarize_stat_at_patch}
\title{Summarize a site-level statistic over patch membership}
\usage{
summarize_stat_at_patch(
  residue_df,
  stat_col = "site_entropy",
  patch = "codon_patch",
  key = "codon_id",
  method = "mean",
  na.rm = TRUE
)
}
\arguments{
\item{residue_df}{Data frame containing a site-level statistic and patch
membership information.}

\item{stat_col}{Character; name of the numeric column to summarize
(default \code{"site_entropy"}).}

\item{patch}{Character; name of the column containing patch membership as a
\code{"+"}-separated string of keys (default \code{"codon_patch"}).}

\item{key}{Character; name of the column used to match patch members to rows
(default \code{"codon_id"}).}

\item{method}{Character; summary method. One of \code{"mean"}, \code{"min"},
\code{"max"}, or \code{"median"} (default \code{"mean"}).}

\item{na.rm}{Logical; if \code{TRUE} (default), ignore \code{NA} values when
summarizing.}
}
\value{
\code{residue_df} with a new numeric column named
  \code{paste0(method, "_", stat_col)} containing patch-level summaries.
}
\description{
Compute a per-row patch summary of a site-level statistic (e.g., entropy,
diversity) using a patch-membership column that encodes member keys as a
\code{"+"}-separated string. For each row, patch members are matched to
\code{key} and the requested summary statistic is computed across
\code{stat_col}.
}
