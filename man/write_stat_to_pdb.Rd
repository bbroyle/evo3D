% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ANALYSIS_UTILS.R
\name{write_stat_to_pdb}
\alias{write_stat_to_pdb}
\title{Write selection statistics to PDB B-factors and occupancy}
\usage{
write_stat_to_pdb(
  evo3d_results,
  pdb_id = "pdb1",
  stat_name = "tajima",
  outfile = "test.pdb",
  mapped_chains_only = TRUE,
  adjust_NA_stats = -99
)
}
\arguments{
\item{evo3d_results}{An \code{evo3D_results} object returned by \code{run_evo3d()}.
Must include \code{evo3d_df} and \code{pdb_info_sets}.}

\item{pdb_id}{Character; PDB identifier to annotate (e.g., \code{"pdb1"}).}

\item{stat_name}{Character vector of length 1 or 2 naming column(s) in
\code{evo3d_results$evo3d_df}.}

\item{outfile}{File path for the modified PDB output. If the file already exists,
a timestamp is appended to avoid overwriting.}

\item{mapped_chains_only}{Logical; if \code{TRUE} (default), retain only chains
mapped in the analysis for the specified PDB.}

\item{adjust_NA_stats}{Numeric; value written where statistics are missing
(default \code{-99}).}
}
\value{
Invisibly returns the output file path. Writes a PDB file to \code{outfile}
  with \code{b} and \code{q} replaced by statistic values.
}
\description{
Embed selection statistics (e.g., Tajima's D, nucleotide diversity) into the
\code{b} (temperature factor) and occupancy (\code{q}) fields of a PDB file for
visualization in PyMOL, Chimera, or other structure viewers. In PyMOL, these
fields can be visualized with commands such as \code{spectrum b} and
\code{spectrum q}.
}
\details{
If two statistics are provided, the first is written to \code{b} and the second
is written to \code{q}.
}
