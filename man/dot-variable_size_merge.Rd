% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EXTENSION_UTILS.R
\name{.variable_size_merge}
\alias{.variable_size_merge}
\title{merge codon-level patches with variable size rules}
\usage{
.variable_size_merge(
  codon_df,
  residue_df,
  merge_type = c("distance", "exposure_distance"),
  only_exposed_in_patch = FALSE
)
}
\arguments{
\item{codon_df}{data frame of codons with columns \code{codon_id}, \code{exposed}, and \code{codon_patch}}

\item{residue_df}{data frame of residues with columns \code{codon_id} and \code{codon_patch}}

\item{merge_type}{character. one of \code{"distance"} (default) or \code{"exposure_distance"}}

\item{only_exposed_in_patch}{logical. if TRUE, all members must be exposed;
if FALSE, only seed codons must be exposed}
}
\value{
updated \code{codon_df} with merged \code{codon_patch} column
}
\description{
rebuilds codon patches from residue-level assignments using either
geometric distance only, or exposure + distance criteria.
}
\details{
three modes are supported:
\itemize{
  \item distance – patches merged by geometry only
  \item exposure_distance – seeds must be exposed, members may be buried
  \item exposure_distance + only_exposed_in_patch = TRUE – all residues must be exposed
}
}
\keyword{internal}
