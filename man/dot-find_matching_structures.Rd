% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PDB_MODULE.R
\name{.find_matching_structures}
\alias{.find_matching_structures}
\title{find matching pdb structures}
\usage{
.find_matching_structures(
  pep,
  identity_cutoff = 80,
  max_hits = 5,
  generate_plot = T,
  download_pdbs = T,
  output_dir = "retrieved_pdbs"
)
}
\arguments{
\item{pep}{protein sequence as a single character string}

\item{identity_cutoff}{minimum sequence identity percentage (default 80)}

\item{max_hits}{maximum number of structures to return (default 5)}

\item{generate_plot}{logical, whether to return a ggplot summary (default TRUE)}

\item{download_pdbs}{logical, whether to download matching pdbs (default TRUE)}

\item{output_dir}{directory to save downloaded structures (default "retrieved_pdbs")}
}
\value{
a list with elements:
\describe{
  \item{top_hits}{data frame of top pdb hits passing filters}
  \item{all_hits}{data frame of all blast hits with status annotations}
  \item{hit_plot}{ggplot object if \code{generate_plot=TRUE}, else NULL}
  \item{pdb_table}{table of downloaded pdb files if \code{download_pdbs=TRUE}, else NULL}
}
}
\description{
blast a protein sequence against the rcsb pdb to find candidate structures.
returns top hits filtered by sequence identity, with option to download pdbs
and generate a hit summary plot.
useful when no structure is supplied and you want evo3d to suggest starting models.
}
\details{
this is a wrapper for \code{bio3d::blast.pdb()} and \code{bio3d::get.pdb()}.
results are filtered on sequence identity, then the top hits are kept.
external database queries can be rate limited, so avoid running in tight parallel loops.
}
\seealso{
\code{\link{download_structures}}, \code{bio3d::blast.pdb}, \code{bio3d::get.pdb}
}
\keyword{experimental}
